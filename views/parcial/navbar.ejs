<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="https://t.me/username1223334">BootesVoid</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="/"><i class="fa fa-home"></i> Home <span class="sr-only">(current)</span></a>
        </li>
        <% if(user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/add"><i class="fa fa-plus"></i> Add</a>
          </li>
        <% } %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user text-<%- user ? "success" : "" %> "></i> 
            <%- user ? user.name : " User" %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if(!user) { %>
            <a class="dropdown-item" href="/signup"><i class="fa fa-list-alt"></i> Sign Up</a>
            <a class="dropdown-item" href="/signin"><i class="fa fa-sign-in"></i> Sign In</a>
            <% } else if(user) { %>
              <a class="dropdown-item" href="/signout"><i class="fa fa-sign-out"></i> Sign Out</a>
            <% } %>
          </div>
        </li>
      </ul>
    </div>
  </nav>